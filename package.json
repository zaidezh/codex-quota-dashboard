{
  "name": "codex-quota-dashboard-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/frontend-contract.test.mjs",
    "test:python": "python -m unittest discover -s tests -p test_*.py",
    "test:e2e": "node tests/e2e.mjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
